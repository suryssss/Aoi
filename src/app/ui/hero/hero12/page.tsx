import React from 'react';
import Hero12 from '@/components/Hero/Hero12';

export const metadata = {
    title: 'Hero 12 | Practice App',
};

const Page = () => {
    return (
        <main>
            <Hero12 />
        </main>
    );
};

export default Page;
